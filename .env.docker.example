# Docker Environment Variables Example
# Copy this file to .env for docker-compose or set these in your deployment environment

# ============================================
# REQUIRED VARIABLES
# ============================================

# JWT secret for authentication (MUST be changed in production)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# ============================================
# DATABASE CONFIGURATION
# ============================================

# SQLite (default - uses local file in container)
DATABASE_URL=file:/app/prisma/db/prod.db

# PostgreSQL example (external database)
# DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# MySQL example (external database)
# DATABASE_URL=mysql://user:password@host:3306/database

# ============================================
# APPLICATION CONFIGURATION
# ============================================

# Public URL of your application
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment
NODE_ENV=production

# Default from email address
EMAIL_FROM=noreply@crunchycone.app

# ============================================
# OPTIONAL: EMAIL PROVIDER CONFIGURATION
# ============================================

# SendGrid
# SENDGRID_API_KEY=your-sendgrid-api-key

# Resend
# RESEND_API_KEY=your-resend-api-key

# AWS SES
# AWS_SES_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ============================================
# DOCKER-SPECIFIC SETTINGS
# ============================================

# Port mapping (host:container)
# Change if port 3000 is already in use
# PORT=3000

# ============================================
# POSTGRES CONFIGURATION (if using external DB)
# ============================================

# POSTGRES_USER=crunchycone
# POSTGRES_PASSWORD=changeme
# POSTGRES_DB=crunchycone_db